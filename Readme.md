# Arch Linux Btrfs Partitioner

Этот скрипт автоматизирует «грязную» часть установки Arch Linux: разметку диска через GPT и создание структуры подтомов Btrfs с правильными опциями монтирования.

## 🛠 Предварительные шаги

1. **Загрузись с Arch ISO.**
2. **Настрой интернет:**

- Если по проводу — всё должно работать сразу.
- Если по Wi-Fi, используй `iwctl`:

```bash
iwctl
station wlan0 scan
station wlan0 get-networks
station wlan0 connect "ТВОЯ_СЕТЬ"
exit

```

3. **Проверь соединение:** `ping -c 3 google.com`.

---

## Запуск скрипта

Прямо в Live-окружении выполни эти команды:

### 1. Скачай скрипт

Используй `curl` (замени `URL_С_GITHUB` на прямую ссылку к твоему Gist или репозиторию):

```bash
curl -L URL_С_GITHUB -o partition.sh

```

### 2. Запусти установку

Скрипт интерактивно спросит, какой диск использовать (например, `/dev/nvme0n1`), и попросит подтверждение.

```bash
bash partition.sh

```

---

- Создаст таблицу разделов **GPT**.
- Разметит **512MB EFI** и всё остальное под **Btrfs**.
- Создаст подтома: `@`, `@home`, `@log`, `@pkg`, `@tmp`, `@snapshots`.
- Примонтирует их в `/mnt` с опциями `zstd:3` и `discard=async`.
